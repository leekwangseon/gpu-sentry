# Offline gpu-burn source

For air-gapped installation, place a source archive at
`tools/gpu-burn/gpu-burn.tar.gz`. The archive must contain the upstream
gpu-burn source tree, either at its root or in one top-level directory.

The archive is intentionally not vendored. Review and pin the upstream source
before distributing it in a controlled environment.

